/** Adds two given numbers, returning the sum.
 * @param {string} id
 * @param {object[]} arrayOfObjects
 * @returns {object[]} the sum of a and b
 */
function toggle(id, arrayOfObjects) {
    return arrayOfObjects
}

export { toggle };
